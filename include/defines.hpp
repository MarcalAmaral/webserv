#ifndef DEFINES_HPP
# define DEFINES_HPP

// Defined in /etc/protocols
# define TCP_PROTOCOL 5

# define MAX_EVENTS 10
# define BUFFER_SIZE 4096

enum e_exit {
    SUCCESS = 0,
    FAIL = 1,
};

#endif // DEFINES_HPP